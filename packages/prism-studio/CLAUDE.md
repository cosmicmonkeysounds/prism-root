# @prism/studio

The Universal Host — Vite SPA + Tauri 2.0 desktop shell + Capacitor mobile wrapper.

## Build
- `pnpm dev` — Vite dev server on :1420
- `pnpm build` — production build
- `pnpm tauri dev` — Tauri desktop with hot reload
- `pnpm typecheck`
- `pnpm exec cap sync ios|android` — sync built `dist/` into native shells
- `pnpm exec cap add ios|android` — one-time scaffold of native projects (not checked in)

## Architecture
- Vite SPA (NOT Next.js) — local-first philosophy
- Tauri 2.0 shell wraps the SPA for desktop
- Capacitor 7 wraps the same SPA for iOS/Android (`capacitor.config.ts`)
- All daemon communication via `src/ipc-bridge.ts` using Tauri invoke()
- Never raw HTTP between frontend and daemon

## Self-replication (App Builder)
Studio composes focused apps (Flux / Lattice / Cadence / Grip / Relay) and
produces web / Tauri / Capacitor builds from the same codebase. Pipeline:
1. `BuilderManager` (`@prism/core/builder`) composes a `BuildPlan` from an
   `AppProfile` + `BuildTarget`. Studio just consumes it.
2. Each step is dispatched to the Prism Daemon via Tauri IPC
   (`invoke("run_build_step", { step, workingDir, env })`).
3. The daemon (`packages/prism-daemon/src/commands/build.rs`) resolves
   paths against `workingDir`, executes the step (emit-file, run-command,
   or invoke-ipc), and returns captured stdout/stderr.
4. For mobile targets, native scaffolds under `ios/` and `android/` are
   generated by running `pnpm cap add ios|android` once per checkout.
   Those directories are gitignored.

## Kernel (`src/kernel/`)
The Studio kernel wires all `@prism/core` subsystems together. All support modules (relay manager, builder manager, design tokens, page export, block style) were extracted to `@prism/core` in ADR-002 Phase 3 — Studio only keeps wiring + entity definitions here.
- `studio-kernel.ts` — `createStudioKernel({ lensBundles?, initializers? })`. Creates and connects ObjectRegistry, CollectionStore, PrismBus, AtomStore, ObjectAtomStore, UndoRedoManager, NotificationStore, SearchEngine, ActivityStore, ActivityTracker, RelayManager (from `@prism/core/relay-manager`), BuilderManager (from `@prism/core/builder`), AutomationEngine, graph analysis, expression evaluator, PluginRegistry, InputRouter, VaultRoster, FormState helpers, Identity (DID generation/sign/verify/export/import), VfsManager (content-addressed blob storage with locks), Trust & Safety (PeerTrustGraph, SchemaValidator, LuauSandbox, ShamirSplitter, EscrowManager), Facet System (FacetParser, SpellChecker, ProseCodec, Sequencer, Emitters, FacetDefinition registry), DesignTokenRegistry (from `@prism/core/design-tokens`). Also wires clipboard (copy/cut/paste), batch ops, templates, LiveView, **and the lens system**: the kernel owns its own `LensRegistry`, `lensComponents: Map<LensId, ComponentType>`, and `shellStore` (zustand). Callers pass `lensBundles` (typically `createBuiltinLensBundles()`) and each bundle self-registers its manifest + React component. `App.tsx` no longer touches lens plumbing — it just reads `kernel.lensRegistry`, `kernel.lensComponents`, `kernel.shellStore`. `dispose()` tears down every installed bundle + initializer in reverse order.
- `initializer.ts` — thin Studio-specialized alias over `@prism/core/initializer`. Exports `StudioInitializer = KernelInitializer<StudioKernel>` + re-exports `installInitializers` from Core. The generic `KernelInitializer<TKernel>` pattern lives in Core so Flux/Lattice/Musica can specialise it with their own kernel types.
- `builtin-initializers.ts` — the canonical `createBuiltinInitializers()` list: `pageTemplatesInitializer` (blog + landing page templates), `sectionTemplatesInitializer` (hero / CTA / pricing / footer), `demoWorkspaceInitializer` (seeds Home + About pages into an empty store, clears undo history, selects the home page), `dynamicDataInitializer` (seeds ~25 sample dynamic records — tasks, reminders, contacts, events, notes, goals, habits, bookmarks, captures — guarded on an empty store for those record types so it never clobbers user data).
- `entities.ts` — page-builder entity types (folder, page, section, heading, text-block, image, button, card, luau-block, facet-view, spatial-canvas, data-portal) + **record types** (task, reminder, contact, event, note, goal, habit, bookmark, timer-session, capture) + **dynamic widgets** (tasks-widget, reminders-widget, contacts-widget, events-widget, notes-widget, goals-widget, habit-tracker-widget, bookmarks-widget, timer-widget, capture-inbox-widget) with category containment rules and edge types (references, links-to). The `record` category (`canParent: []`, `canBeRoot: true`) hosts free-standing data rows at the workspace root; dynamic widgets are `component` (`childOnly: true`) that read records via `kernel.store.allObjects()`. Imports `STYLE_FIELD_DEFS` from `@prism/core/page-builder`.
- `kernel-context.tsx` — React context + hooks: useKernel, useSelection, useObjects, useObject, useUndo, useNotifications, useRelay, useConfig, useConfigSettings, usePresence, useAutomation, useGraphAnalysis, useExpression, usePlugins, useInputRouter, useVaultRoster, useIdentity, useVfs, useTrust, useFacetParser, useSpellCheck, useProseCodec, useSequencer, useEmitters, useFacetDefinitions

## Components (`src/components/`)
- `studio-shell.tsx` — custom shell layout replacing core ShellLayout with real sidebar/inspector content
- `object-explorer.tsx` — tree view of objects from CollectionStore, click to select, "New Page" button, search via SearchEngine. Includes TemplateGallery overlay, ActivityFeed sidebar, reorder buttons (↑/↓) on selected nodes, and HTML5 drag-drop for reorder/reparent with containment validation. Pure navigation tree — data views (list/table/card/kanban/report) live as Puck widgets composed into pages.
- `component-palette.tsx` — sidebar listing available block types (component/section) from ObjectRegistry, grouped by category, searchable, click-to-add with containment validation, draggable items. Wired into StudioShell sidebar below ObjectExplorer.
- `inspector-panel.tsx` — schema-driven property editor reading EntityDef fields from ObjectRegistry. Includes clipboard buttons (Copy/Cut/Paste), Add Child menu, and Expression Bar for formula evaluation.
- `notification-toast.tsx` — floating toast overlay subscribing to NotificationStore
- `undo-status-bar.tsx` — undo/redo buttons in the header bar
- `presence-indicator.tsx` — colored avatar dots for connected peers in header bar
- `spatial-canvas-renderer.tsx` — free-form absolute-positioned layout using react-moveable + react-selecto
- `facet-view-renderer.tsx` — renders FacetDefinition in form/list/table/report/card view modes
- `data-portal-renderer.tsx` — related records inline table via edge relationships
- `button-renderer.tsx` — real `<button>`/`<a>` Puck preview with variants (primary/secondary/outline/ghost/danger/success/gradient), sizes (xs–xl), leading/trailing icon, fullWidth, disabled, loading spinner, rounded/shadow/hoverEffect presets, target + auto-`noopener noreferrer`, button type, aria label; exports `resolveVariant`, `resolveSize`, `resolveRadius`, `resolveShadow`, `resolveTransform`, `buildButtonStyles`, `resolveRel`
- `card-renderer.tsx` — real card Puck preview with variants (elevated/outlined/filled/ghost), layouts (vertical/horizontal/overlay), eyebrow/title/body/CTA (reuses `ButtonRenderer`), media fit + aspect ratio, hover effect; exports `resolveCardVariant`, `resolveCardLayout`, `clampOverlayOpacity`, `buildCardStyles`
- `form-input-renderers.tsx` — primitive Puck widgets for text/textarea/select/checkbox/number/date inputs; each is an uncontrolled preview with its own local state and exports `parseSelectOptions`
- `layout-primitive-renderers.tsx` — columns (1-6 col grid), divider (with optional centered label), spacer (vertical/horizontal); exports `clampColumns`
- `data-display-renderers.tsx` — stat-widget (KPI over kernel objects), badge, alert, progress-bar; exports `computeStat`, `formatStatValue`, `progressRatio`, and the `BadgeTone` palette
- `content-renderers.tsx` — markdown-widget (dependency-free md→HTML with escaping) and iframe-widget (http/https only, sandboxed); exports `renderMarkdown`, `renderInlineMarkdown`, `isSafeIframeUrl`
- `code-block-renderer.tsx` — static `<pre><code>` code block with language label, caption, line-number gutter, wrap toggle; exports `splitCodeLines`, `gutterWidth`
- `media-renderers.tsx` — video-widget + audio-widget backed by native HTML5 media, http(s)-only allow-list; exports `isSafeMediaUrl`, `clampPx`
- `list-widget-renderer.tsx` — data-driven list widget (filtered by collectionType); exports pure `readListField()` for shell/data field lookup
- `table-widget-renderer.tsx` — sortable columnar table widget driven by a `columns` spec (`"id:Label, id:Label"`); exports `parseTableColumns`, `readCellValue`, `sortObjects`, `TableSortDir`
- `card-grid-widget-renderer.tsx` — responsive CSS-grid card view; exports `clampColumnWidth`
- `report-widget-renderer.tsx` — grouped/aggregated report widget (count/sum/avg/min/max); exports `buildReportGroups`, `computeAggregate`, `formatAggregate`
- `chart-widget-renderer.tsx` — **recharts**-backed bar / line / pie / area charts inside a `<ResponsiveContainer>`. Pure aggregation/palette logic lives in the sibling `chart-data.ts` (`aggregateObjects`, `CHART_PALETTE`, types `ChartType`/`ChartAggregation`/`ChartDataPoint`) so vitest can import it without evaluating recharts in the node env. The renderer re-exports `chart-data.ts` for back-compat.
- `chart-data.ts` — pure helpers extracted from the chart renderer for test use; no React imports.
- `map-widget-renderer.tsx` — **react-leaflet** map widget (`MapContainer`/`TileLayer`/`Marker`/`Popup`) over OpenStreetMap tiles. Patches Leaflet's default icon URLs by importing the PNGs from `leaflet/dist/images/*` (declared in `src/vite-env.d.ts`). Custom inline `FitBounds` component uses `useMap()` to fit-to-markers when bounds change. Click handlers use a conditional spread (`{...(onSelectObject ? { eventHandlers: ... } : {})}`) so `exactOptionalPropertyTypes` doesn't reject `undefined` for `LeafletEventHandlerFnMap`. **Does not import `leaflet/dist/leaflet.css`** — host apps (studio, puck-playground) import it from their own `main.tsx` so vitest stays DOM-free.
- `map-data.ts` — pure marker/bounds helpers (`extractMarkers`, `computeBounds`, types `MapMarker`/`MapBounds`) split from the renderer for the same vitest reason as `chart-data.ts`.
- `dynamic-widget-renderers.tsx` — ten specialised record-type widgets (`TasksWidgetRenderer`, `RemindersWidgetRenderer`, `ContactsWidgetRenderer`, `EventsWidgetRenderer`, `NotesWidgetRenderer`, `GoalsWidgetRenderer`, `HabitTrackerWidgetRenderer`, `BookmarksWidgetRenderer`, `TimerWidgetRenderer`, `CaptureInboxWidgetRenderer`). Each queries `kernel.store.allObjects()` filtered by record type and renders domain-specific UX (task checkbox rows with inline done-toggle, reminder round-checkboxes with repeat glyph, contact cards with avatar + mailto:/tel: actions, event timeline with DateChip + hh:mm, note previews with pinned star + tag chips, goal progress bars, habit weekly-ratio bars + streak fire, bookmark favicon grid, local focus-timer with session log, quick-capture inbox with inline add + mark-processed). All pure helpers are exported for vitest: `parseObjectDate`, `formatRelativeDate`, `filterTasks`/`orderTasks`/`priorityColor`, `filterReminders`, `filterContacts`/`contactInitials`, `filterEvents`/`formatEventTime`, `filterNotes`/`notePreview`, `filterGoals`/`goalRatio`, `formatDuration`, `filterBookmarks`/`bookmarkHost`, `habitWeeklyRatio`, `filterCaptures`. `nowMs()` is injected so tests can freeze time. Records are backed by the new `record` category in `entities.ts` (tasks/reminders/contacts/events/notes/goals/habits/bookmarks/timer-sessions/captures). See `docs/adr/003-dynamic-data-providers.md` for Tier 2 (external-stream providers) scoping.
- `peer-cursors-overlay.tsx` — Tier 8E collaboration: `PeerCursorsBar` (rendered at the top of `canvas-panel`) + `PeerSelectionBadge` + pure `groupPeerSelections()` helper. All state driven by `usePresence()` off `@prism/core/presence`.

## Panels (`src/panels/`)
- `canvas-panel.tsx` — WYSIWYG page preview: renders page→section→component tree as visual React components, click-to-select blocks, floating block toolbar (move/duplicate/delete) on selected blocks, quick-create combobox for adding allowed child types. Renders `luau-block` objects inline via Luau UI parser.
- `editor-panel.tsx` — CodeMirror 6 editing selected text-block/heading content (or scratch buffer when nothing editable selected)
- `graph-panel.tsx` — @xyflow/react node graph of kernel objects and edges, live-reactive (subscribes to store changes, rebuilds on mutations)
- `layout-panel.tsx` — Puck visual builder wired to kernel: generates config from ObjectRegistry, projects kernel objects to Puck Data, diffs Puck changes back to kernel CRUD. Live onChange sync (debounced 300ms). Luau Block component renders Luau UI inline via parser.
- `crdt-panel.tsx` — CRDT state inspector (objects/edges/JSON tabs)
- `relay-panel.tsx` — Relay Manager: add/remove relays, connect/disconnect, publish/unpublish portals, view portal URLs, CLI reference
- `settings-panel.tsx` — Category-grouped settings UI from ConfigRegistry with search, toggle/select/number/string inputs
- `automation-panel.tsx` — Automation rules: create/edit/delete/toggle/run automations with trigger/condition/action config, run history with status badges
- `analysis-panel.tsx` — Graph analysis: critical path (CPM), cycle detection, blocking chain, impact analysis, slip impact calculator
- `plugin-panel.tsx` — Plugin registry: register/remove plugins, view contributions (commands/views), expand details
- `shortcuts-panel.tsx` — Keyboard binding manager: view/add/remove bindings, input scopes, event log
- `vault-panel.tsx` — Vault roster: add/remove/pin/open vaults, search filtering, pinned section
- `identity-panel.tsx` — Identity management: generate DID, display DID/document/public key, sign & verify payloads, export/import JSON
- `assets-panel.tsx` — VFS browser: import files, browse blobs (hash/size/MIME), lock/unlock binary forking, remove files
- `trust-panel.tsx` — Trust dashboard: 4 tabs — Peers (add/trust/distrust/ban/unban with trust levels), Validation (JSON schema validator), Flags (content hash flagging), Escrow (deposit/list encrypted key material)
- `form-facet-panel.tsx` — Schema-driven form renderer: YAML/JSON source editor, auto-detected fields, bidirectional source↔form sync, spell checking
- `table-facet-panel.tsx` — Data grid: sortable/filterable columns, inline editing, keyboard navigation, row selection
- `sequencer-panel.tsx` — Visual automation builder: condition builder (subject/operator/value), script builder (step list), live Luau preview
- `report-facet-panel.tsx` — Grouped/summarized data view with aggregates (count, sum, avg, min, max), collapsible groups
- `luau-facet-panel.tsx` — Luau `ui.*` call parser → UINode tree → React renderer, 10 element types, sample scripts. Kernel-bound: selecting a `luau-block` object auto-loads its source; edits auto-save to kernel (debounced 400ms). Exports `parseLuauUi()` and `renderUINode()` for reuse by Canvas/Layout panels.
- `facet-designer-panel.tsx` — Visual layout builder for FacetDefinitions (parts, slots, summary fields, sort/group, automation hooks)
- `spatial-canvas-panel.tsx` — Free-form layout editor: field palette, slot inspector, grid/snap, alignment tools. Lens #23 (Shift+X)
- `visual-script-panel.tsx` — FileMaker-style step-based script editor: categorized step palette (31 step types across 7 categories), drag-to-add, per-step parameter inputs, move/disable/remove controls, indented control flow blocks, live Luau preview via emitStepsLuau(), block validation. Lens #24 (Shift+S)
- `saved-view-panel.tsx` — Found Set manager: create/delete/pin/search saved views, filter summary display, active view highlighting. Found Sets drive list/table/card-grid/report widgets composed into pages. Lens #25 (Shift+V)
- `value-list-panel.tsx` — Value list editor: CRUD for static lists (inline value/label/color rows) and dynamic lists (collection source, value/display/sort fields). Lens #26 (Shift+L)
- `privilege-set-panel.tsx` — Access control manager: collection/field/layout permission matrix, row-level security expression, DID-to-role assignment, default/admin options. Lens #27 (Shift+P)
- `publish-panel.tsx` — Publish workflow + HTML/JSON export + read-only preview mode. Resolves the active page from selection (walks parentId), drives a `draft → review → published` state machine on the page object, exports via `@prism/core/page-builder`. Lens #29 (Shift+U, 🚀)
- `design-tokens-panel.tsx` — CSS variable / theme picker driven by `@prism/core/design-tokens` (colors/spacing/fonts). Lens (Shift+T)
- `form-builder-panel.tsx` — Composes `@prism/core/forms` form-input entities under the nearest container ancestor (section/page/card/columns/spatial-canvas). Walks parent chain to pick a container. Lens (Shift+G)
- `site-nav-panel.tsx` — Multi-page navigation editor. Every `page` object in the vault with set-home / hide / reorder controls. Exports pure `buildSiteNav()` for the `site-nav` widget + breadcrumbs. Lens (Shift+N)
- `entity-builder-panel.tsx` — UI for authoring new `EntityDef`s at runtime (type name, category, icon, color, fields). Registers into `kernel.registry`, types immediately appear in the component palette. Lens (Shift+E)
- `relationship-builder-panel.tsx` — UI for authoring `EdgeTypeDef`s (relation id, label, behavior, color, source/target type restrictions). Uses conditional assignment for `exactOptionalPropertyTypes`. Lens (Shift+R)

## Lenses
Every panel self-registers via a `LensBundle` colocated with its component. A panel file (e.g. `panels/editor-panel.tsx`) exports both its React component and a `xxxLensBundle = defineLensBundle(manifest, Component)` created with `defineLensBundle` from `src/lenses/bundle.ts` (Studio's `ComponentType`-specialized re-export of `@prism/core/lens`'s generic `defineLensBundle`). `src/lenses/index.tsx` is a pure aggregator: it imports each bundle and exposes `createBuiltinLensBundles()`. The kernel installs them via `installLensBundles` inside `createStudioKernel`, so manifests + components land in `kernel.lensRegistry` + `kernel.lensComponents` atomically — no parallel registries, no manifest list to sync.

Adding a new lens:
1. Create `panels/my-panel.tsx` exporting `MyPanel` and `myLensBundle`.
2. Add `myLensBundle` to the list in `lenses/index.tsx`.

Core keybindings: Editor (e), Graph (g), Layout (l), Canvas (v), CRDT (c), Relay (r), Settings (,), Automation (a), Analysis (n), Plugins (p), Shortcuts (k), Vaults (w), Identity (i), Assets (f), Trust (t), Form (d), Table (b), Sequencer (q), Report (o), Luau Facet (u), Facet Designer (x), Spatial Canvas (Shift+X), Visual Script (Shift+S), Saved Views (Shift+V), Value Lists (Shift+L), Privilege Sets (Shift+P), App Builder (Shift+B), Import (Shift+Y), Publish (Shift+U), Design Tokens (Shift+T), Form Builder (Shift+G), Site Navigation (Shift+N), Entity Builder (Shift+E), Relationships (Shift+R)

## Data Flow
```
User action → kernel.createObject/updateObject/deleteObject
  → CollectionStore.putObject (Loro CRDT)
  → PrismBus.emit (ObjectCreated/Updated/Deleted)
  → connectBusToObjectAtoms → ObjectAtomStore (Zustand cache)
  → React components re-render via useSyncExternalStore
  → UndoRedoManager.push (snapshot for undo)
  → SearchEngine auto-reindexes via CollectionStore.onChange
```

## Key Design Decisions
- Kernel is a singleton created at app startup, not per-component
- All mutations go through kernel CRUD methods (never raw CollectionStore)
- Bus payloads follow connectBusToObjectAtoms format: `{ object }`, `{ edge }`, `{ id }`
- StudioShell replaces core ShellLayout to inject real sidebar/inspector content
- Panels access data via kernel context (useKernel), not prop drilling
- Editor creates per-object LoroText buffers keyed as `obj_content_{id}`, debounce-syncs back to kernel
- Canvas resolves the selected page (walking up parentId if a child is selected) and renders live
- Studio has NO server code — Relay servers are managed via CLI, Studio connects as a client
- RelayManager uses HTTP for portal CRUD, WebSocket (RelayClient SDK) for live CRDT sync
