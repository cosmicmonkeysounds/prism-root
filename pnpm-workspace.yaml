# The Clay migration retired the last JS packages. `prism-relay`
# was rewritten in Rust on 2026-04-15 (axum Sovereign Portal SSR)
# and moved into the top-level Cargo workspace alongside every
# other crate. `prism-studio` still carries a `package.json` stub
# so pnpm keeps its directory visible to editor tooling, but no
# install step hits that subtree.
packages:
  - "packages/prism-studio"
