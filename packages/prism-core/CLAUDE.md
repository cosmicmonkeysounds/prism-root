# @prism/core

Client-side execution environment — Layer 1 (agnostic TS) + Layer 2 (React renderers).

## Build
- `pnpm typecheck` / `pnpm test` / `pnpm test:watch`

## Architecture
- **Layer 1**: Object Model (GraphObject, ObjectRegistry, TreeModel, EdgeModel, WeakRefEngine, ContextEngine, NSID), Lens System (LensRegistry, ShellStore), Plugin System (PrismPlugin, PluginRegistry, ContributionRegistry), Reactive Atoms (PrismBus, AtomStore, ObjectAtomStore, bus-to-atom bridges), State Machines (Machine, createMachine — flat FSM with guards/actions/lifecycle), Graph Analysis (dependency graph, topological sort, cycle detection, blocking chains, slip impact), Planning Engine (CPM — critical path, early/late start/finish, float), Input System (KeyboardModel, InputScope, InputRouter), Layout System (SelectionModel, PageModel, PageRegistry, LensSlot, LensManager), Forms & Validation (FieldSchema, DocumentSchema, FormState, wiki-links, markdown), Expression Engine (Scanner, Parser, Evaluator), Automation Engine (AutomationEngine, triggers/conditions/actions, condition evaluator, template interpolation), Manifest (PrismManifest — weak references to Collections inside a Vault, StorageConfig, SchemaConfig, SyncConfig, CollectionRef), Config System (ConfigRegistry, ConfigModel, FeatureFlags — layered scope resolution: default→workspace→user), Undo/Redo (UndoRedoManager — snapshot-based undo with merge/batch, createUndoBridge for auto-recording TreeModel/EdgeModel mutations), Server Factory (generateRouteSpecs, buildOpenApiDocument — framework-agnostic REST route generation + OpenAPI 3.1.0 from ObjectRegistry), CRDT Persistence (CollectionStore — Loro-backed object/edge storage with filtering and sync, VaultManager — manifest-driven collection lifecycle with PersistenceAdapter), Search Engine (SearchIndex — TF-IDF inverted index with field-weighted scoring, SearchEngine — cross-collection search orchestrator with structured filters, facets, pagination, live subscriptions), Vault Discovery (VaultRoster — persistent registry of known vaults with sort/pin/search/persistence, VaultDiscovery — filesystem scanning for .prism.json manifests with roster merge and discovery events), Derived Views (ViewRegistry — 7 view modes with capability descriptors, ViewConfig — filter/sort/group pipeline with 12 filter operators, LiveView — auto-updating materialized projection from CollectionStore with facets/groups/subscriptions), Notification System (NotificationStore — in-memory notification registry with 8 kinds, read/dismiss/pin, filtering, eviction policy, subscriptions; NotificationQueue — debounced batching with dedup by objectId+kind), Activity Log (ActivityStore — append-only per-object audit trail with 20 verbs, ring buffer, subscriptions; ActivityTracker — auto-derives events from GraphObject diffs via TrackableStore; formatActivity/formatFieldName/formatFieldValue/groupActivityByDate — human-readable rendering), Batch Operations (createBatchTransaction — atomic multi-op execution with validation, rollback, progress callbacks, single undo entry), Clipboard (createTreeClipboard — cut/copy/paste for GraphObject subtrees with deep clone, ID remapping, internal edge preservation, undo integration), Template System (createTemplateRegistry — reusable ObjectTemplate blueprints with TemplateNode tree, TemplateEdge, variable interpolation {{name}}, instantiation, createFromObject round-trip), Ephemeral Presence (createPresenceManager — RAM-only peer state with cursor/selection/activeView tracking, TTL eviction, subscribe for join/update/leave events), Identity (createIdentity — W3C DID document wrapper for did:key/did:web with Ed25519 keypair generation, resolveIdentity — DID-to-public-key resolution, signPayload/verifySignature — Ed25519 sign/verify, createMultiSigConfig/assembleMultiSignature/verifyMultiSignature — threshold multi-sig for shared vault ownership), Encryption (createVaultKeyManager — HKDF-derived AES-GCM-256 vault keys from Ed25519 identity, encryptSnapshot/decryptSnapshot — encrypt Loro snapshots at rest, per-collection key derivation with rotation, createMemoryKeyStore + KeyStore interface for secure storage), Virtual File System (createVfsManager — content-addressed blob storage with SHA-256 dedup, BinaryRef for GraphObject.data links, importFile/exportFile, Binary Forking Protocol lock-based editing for non-mergeable files, createMemoryVfsAdapter + VfsAdapter interface), Relay (createRelayBuilder — modular composable relay via builder pattern with .use() chaining, 7 built-in modules: blindMailboxModule E2EE store-and-forward, relayRouterModule zero-knowledge routing, relayTimestampModule cryptographic timestamps, blindPingModule push notifications, capabilityTokenModule scoped access tokens, webhookModule outgoing HTTP on CRDT changes, sovereignPortalModule portal levels 1-4; RelayModule interface for custom modules, RelayContext shared capability registry), Actor System (createProcessQueue — priority-ordered process queue with concurrency control, auto-processing, cancel/prune/dispose, queue events; ActorRuntime interface with createLuaActorRuntime/createSidecarRuntime/createTestRuntime; CapabilityScope zero-trust sandboxing per task), Intelligence Layer (createAiProviderRegistry — pluggable AI with active provider switching; createOllamaProvider local inference, createExternalProvider API bridge for Claude/OpenAI; createContextBuilder object-aware context from graph neighbors; AiHttpClient interface for Layer 1 HTTP abstraction), Syntax Engine (createSyntaxEngine — LSP-like diagnostics/completions/hover for expressions, schema-aware type checking via SchemaContext, type inference from EntityFieldDef→ExprType, Lua .d.lua generation from ObjectRegistry schemas; createExpressionProvider built-in provider, SyntaxProvider interface for custom languages), Communication Fabric (createSessionManager — real-time session lifecycle with participants/tracks/delegation, createTranscriptTimeline — ordered searchable time-indexed segments with finalization, createPlaybackController — transcript-synced media seek, TranscriptionProvider/SessionTransport interfaces for Whisper.cpp/LiveKit/WebRTC, Listener Fallback compute delegation to capable peers), NLE / Timeline (createTimelineEngine — transport/tracks/clips/automation/markers with pluggable clock, createTempoMap — PPQN dual-time with tempo automation and time signatures, createManualClock for testing), Flux Domain (createFluxRegistry — 11 entity schemas, 7 edge types, 8 automation presets, CSV/JSON import/export), Zustand stores, Loro bridge, Lua runtime (wasmoon), XState machines
- **Layer 2**: CodeMirror 6 (LoroText sync), Puck (Loro layout), KBar (focus-depth), Spatial Graph (@xyflow/react + elkjs), Shell (ShellLayout, ActivityBar, TabBar, LensProvider), 3D Viewport (createSceneState Loro-backed scene graph, CadGeometryManager OpenCASCADE.js STEP/IGES import, compileTslGraph TSL→GLSL shader compiler, createGizmoController translate/rotate/scale with undo), OpenDAW Audio (createOpenDawBridge — bridges Prism timeline to OpenDAW audio engine with track/clip/transport/effects/export sync, installOpenDawWorkers, useOpenDawBridge/usePlaybackPosition/useTransportControls/useTrackEffects React hooks)
- Loro CRDT is the hidden buffer. Editors project Loro state.
- Zustand stores subscribe to specific Loro node IDs (atomic)

## Exports
- `@prism/core/layer1` — all Layer 1 primitives
- `@prism/core/layer2` — all Layer 2 renderers
- `@prism/core/stores` — Zustand store factories
- `@prism/core/lua` — browser Lua runtime (wasmoon)
- `@prism/core/codemirror` — CodeMirror + LoroText sync
- `@prism/core/puck` — Puck + Loro layout bridge
- `@prism/core/kbar` — KBar with focus-depth routing
- `@prism/core/graph` — Spatial node graph (PrismGraph, custom nodes/edges, elkjs layout)
- `@prism/core/machines` — XState tool machine (hand/select/edit FSM)
- `@prism/core/object-model` — Object Model (GraphObject, registry, tree, edges, weak refs, NSID, query)
- `@prism/core/lens` — Lens system + shell state (LensManifest, LensRegistry, ShellStore, tab/panel management).
- `@prism/core/shell` — Shell React components (ShellLayout, ActivityBar, TabBar, LensProvider)
- `@prism/core/input` — Input System (KeyboardModel, InputScope, InputRouter)
- `@prism/core/forms` — Forms & Validation (FieldSchema, DocumentSchema, FormState, wiki-links, markdown)
- `@prism/core/layout` — Layout System (SelectionModel, PageModel, PageRegistry, LensSlot, LensManager)
- `@prism/core/expression` — Expression Engine (Scanner, Parser, Evaluator with builtins)
- `@prism/core/plugin` — Plugin System (PrismPlugin, PluginRegistry, ContributionRegistry for views/commands/keybindings/menus)
- `@prism/core/atom` — Reactive Atoms (PrismBus event bus, AtomStore UI state, ObjectAtomStore object/edge cache, bus-to-atom bridges)
- `@prism/core/automaton` — State Machines (Machine flat FSM with guards/actions/lifecycle hooks, createMachine factory with start/restore)
- `@prism/core/graph-analysis` — Dependency Graph (topological sort, cycle detection, blocking chains, slip impact) + Planning Engine (CPM critical path method)
- `@prism/core/automation` — Automation Engine (triggers, conditions, actions, condition evaluator, template interpolation, cron scheduling)
- `@prism/core/manifest` — Prism Manifest (weak references to Collections in a Vault; StorageConfig, SchemaConfig, SyncConfig, CollectionRef, parse/serialise/validate). See manifest-types.ts for Vault/Collection/Manifest/Shell glossary.
- `@prism/core/config` — Config System (ConfigRegistry, ConfigModel, FeatureFlags, validateConfig, coerceConfigValue, schemaToValidator, MemoryConfigStore). Layered scope resolution: default → workspace → user. Built-in settings for ui/editor/sync/ai/notifications.
- `@prism/core/undo` — Undo/Redo (UndoRedoManager — snapshot-based undo/redo with merge for rapid edits, batch support, configurable max history, subscribe for UI reactivity, createUndoBridge for auto-recording TreeModel/EdgeModel mutations)
- `@prism/core/server` — Server Factory (generateRouteSpecs — framework-agnostic RouteSpec[] from ObjectRegistry, registerRoutes + RouteAdapter, buildOpenApiDocument/generateOpenApiJson — OpenAPI 3.1.0 document generation, groupByType, printRouteTable)
- `@prism/core/persistence` — CRDT Persistence (createCollectionStore — Loro-backed object/edge storage with ObjectFilter queries and CRDT sync, createVaultManager — manifest-driven collection lifecycle with lazy loading/dirty tracking/save, PersistenceAdapter interface, createMemoryAdapter)
- `@prism/core/search` — Search Engine (createSearchIndex — TF-IDF inverted index with field-weighted scoring and tokenizer, createSearchEngine — cross-collection search orchestrator with structured filters/facets/pagination/live subscriptions/auto-reindex via CollectionStore change events)
- `@prism/core/notification` — Notification System (createNotificationStore — 8 notification kinds, add/markRead/dismiss/pin/getAll/getUnreadCount with eviction policy and subscriptions, createNotificationQueue — debounced batching with dedup by objectId+kind within configurable window)
- `@prism/core/view` — Derived Views (createViewRegistry — 7 view modes with capability queries, applyFilters/applySorts/applyGroups/applyViewConfig — pure transform pipeline with 12 filter operators, createLiveView — auto-updating materialized projection from CollectionStore with facets/groups/dispose)
- `@prism/core/discovery` — Vault Discovery (createVaultRoster — persistent registry of known vaults with CRUD/sort/pin/search/dedup/persistence via RosterStore, createVaultDiscovery — filesystem scanning for .prism.json manifests with DiscoveryAdapter/roster merge/discovery events, createMemoryRosterStore/createMemoryDiscoveryAdapter for testing)
- `@prism/core/activity` — Activity Log (createActivityStore — append-only per-object audit trail with 20 verbs and ring buffer eviction, createActivityTracker — auto-derives events from GraphObject diffs via TrackableStore duck-typed interface, formatActivity/formatFieldName/formatFieldValue/groupActivityByDate — human-readable text+HTML rendering and date bucketing)
- `@prism/core/batch` — Batch Operations (createBatchTransaction — atomic multi-op execution with 7 op kinds, pre-flight validation, rollback on failure, progress callbacks, single undo entry for entire batch)
- `@prism/core/clipboard` — Clipboard (createTreeClipboard — cut/copy/paste for GraphObject subtrees with deep clone, ID remapping, internal edge preservation, cut=one-time paste+delete, single undo entry)
- `@prism/core/template` — Template System (createTemplateRegistry — reusable ObjectTemplate blueprints with TemplateNode tree/TemplateEdge/TemplateVariable, register/list/filter, instantiate with {{variable}} interpolation, createFromObject for round-trip template creation)
- `@prism/core/presence` — Ephemeral Presence (createPresenceManager — RAM-only peer state tracking with CursorPosition/SelectionRange/activeView, PeerIdentity with color/displayName, receiveRemote for awareness protocol ingestion, TTL-based sweep eviction, subscribe for joined/updated/left events, injectable TimerProvider for testing)
- `@prism/core/identity` — Identity (createIdentity — W3C DID identity with Ed25519 keypair for did:key/did:web, resolveIdentity — resolve DID to public key, signPayload/verifySignature — Ed25519 sign/verify, createMultiSigConfig/createPartialSignature/assembleMultiSignature/verifyMultiSignature — threshold multi-sig, base58btc/multicodec encoding utilities)
- `@prism/core/encryption` — Encryption (createVaultKeyManager — HKDF-derived AES-GCM-256 vault/collection keys with rotation, encryptSnapshot/decryptSnapshot — encrypt Loro snapshots at rest with AAD, createMemoryKeyStore — in-memory KeyStore for testing, KeyStore interface for Tauri keychain integration)
- `@prism/core/vfs` — Virtual File System (createVfsManager — content-addressed blob storage with SHA-256 dedup, BinaryRef for GraphObject.data, importFile/exportFile, Binary Forking Protocol with acquireLock/releaseLock/replaceLockedFile, createMemoryVfsAdapter for testing, VfsAdapter interface for Tauri local FS)
- `@prism/core/relay` — Relay (createRelayBuilder — composable relay via builder pattern, 7 modules: blindMailboxModule/relayRouterModule/relayTimestampModule/blindPingModule/capabilityTokenModule/webhookModule/sovereignPortalModule, RelayModule interface for custom modules, RELAY_CAPABILITIES registry, createMemoryPingTransport for testing)
- `@prism/core/session` — Communication Fabric (createSessionManager — session lifecycle with create/join/end/pause/resume, participant management with roles/muting, media track add/remove/mute, createTranscriptTimeline — sorted segments with search/range/finalization/toPlainText, createPlaybackController — position/speed/seekToSegment with listeners, TranscriptionProvider interface for Whisper.cpp sidecar, SessionTransport interface for LiveKit/WebRTC, Listener Fallback delegation to capable peers, createTestTransport/createTestTranscriptionProvider for testing)
- `@prism/core/syntax` — Syntax Engine (createSyntaxEngine — LSP-like expression diagnostics/completions/hover with schema-aware type checking, inferNodeType for AST type inference, generateLuaTypeDef for .d.lua from ObjectRegistry, createExpressionProvider built-in + SyntaxProvider interface for custom languages, BUILTIN_FUNCTIONS/FIELD_TYPE_MAP constants)
- `@prism/core/actor` — Actor System + Intelligence Layer (createProcessQueue — priority process queue with concurrency/auto-processing/events/cancel/prune, createLuaActorRuntime/createSidecarRuntime/createTestRuntime — pluggable runtimes with CapabilityScope sandboxing, createAiProviderRegistry — multi-provider AI with active switching, createOllamaProvider/createExternalProvider — local + API inference, createContextBuilder — object-aware graph context for prompts, createTestAiProvider for testing)
- `@prism/core/trust` — Trust & Safety (createLuaSandbox — capability-based API restriction with glob URL/path filtering and violation recording, createSchemaValidator — 5 built-in validation rules for import safety, createHashcashMinter/createHashcashVerifier — SHA-256 proof-of-work spam protection, createPeerTrustGraph — peer reputation with trust/distrust/ban and content hash flagging, createShamirSplitter — GF(256) Shamir secret sharing for vault recovery, createEscrowManager — deposit/claim/evict lifecycle with TTL)
- `@prism/core/timeline` — NLE / Timeline System (createTimelineEngine — transport play/pause/stop/seek/scrub/loop with pluggable TimelineClock, track CRUD with 5 TrackKinds audio/video/lighting/automation/midi, clip add/remove/move/trim with lock/mute/gain, automation lanes with step/linear/bezier interpolation, timeline markers, event subscriptions; createTempoMap — dual time model with PPQN musical time and seconds, tempo automation via tempo markers, time signature changes, bar/beat/tick↔seconds conversion; createManualClock — test clock with advance/setTime/schedule/cancel)
- `@prism/core/flux` — Flux Domain (createFluxRegistry — 11 entity types across 4 categories: productivity (Task/Project/Goal/Milestone), people (Contact/Organization), finance (Transaction/Account/Invoice), inventory (Item/Location); 7 edge types (assigned-to/depends-on/blocks/belongs-to/related-to/invoiced-to/stored-at); 8 automation presets (task completion timestamp, recurring reset, overdue notifications, low stock alerts, goal progress); CSV/JSON import/export with field selection)
- `@prism/core/audio` — OpenDAW Audio Bridge (createOpenDawBridge — bridges Prism Layer 1 timeline to OpenDAW audio engine with bidirectional transport sync via AnimationFrame, track loading with AudioFileBox/AudioRegionBox/PPQN conversion, 10 audio effects via EffectFactories, volume/pan/mute/solo control, exportMix/exportStems to WAV via AudioOfflineRenderer; installOpenDawWorkers — one-time worker/worklet setup; useOpenDawBridge — React hook for bridge lifecycle, usePlaybackPosition — AnimationFrame position tracking with musical time, useTransportControls — play/pause/stop/seek handlers, useTrackEffects — per-track audio controls)
- `@prism/core/viewport3d` — 3D Viewport / Builder 3 (createSceneState — Loro-backed scene graph with nodes/materials/hierarchy/transforms/CRDT sync, createCadGeometryManager — OpenCASCADE.js STEP/IGES/BREP import with tessellation and bounding box via CadWorkerAdapter, compileTslGraph — TSL node-wire graph to GLSL compiler with topological sort/type checking/cycle detection targeting WebGL/WebGPU, createGizmoController — translate/rotate/scale gizmo state with snapping and undo integration via GizmoUndoAdapter, createTestCadAdapter for testing)
