# prism-core

Shared Rust foundations for the post-Slint Prism stack. With Slint
exorcised in the Phase 5 cutover (see `docs/dev/clay-migration-plan.md`),
this crate is the workspace's leaf — design tokens, language /
syntax, kernel orchestration, networking — feeding `prism-builder`,
`prism-ui-runtime`, `prism-shell`, and the relay / studio hosts above
it. Modules land leaf-first; the table below tracks status.

## Build & Test
- `cargo build -p prism-core` — default features (no `loro`).
- `cargo build -p prism-core --features crdt` — pulls in `loro` for hosts
  that actually run the CRDT layer.
- `cargo test -p prism-core` — unit + insta snapshot tests.

## Features
- `crdt` — adds the `loro` dependency. Off by default so pure-logic
  consumers (design tokens, shell mode, boot config, puck-JSON parsing)
  don't drag the CRDT engine into their dependency tree.

## Public surface
From `src/lib.rs`:
- `BootConfig`, `DEFAULT_BOOT_CONFIG` — the four-source boot-time
  resolver ported from `src/boot/load-boot-config.ts`.
- `DesignTokens` — color / spacing / typography constants. Leaf module.
- `Permission`, `ShellMode`, `ShellModeContext` — the runtime context
  Studio uses to decide which lenses and panels are reachable.
- `Store<S>`, `Action<S>`, `Subscription` — the zustand replacement
  from `kernel::store`. A single owning container for `S` with
  reducer-style dispatch, a synchronous subscription bus, and
  `snapshot` / `restore` via serde for §7 hot-reload.
- `Atom<T>`, `AtomSubscription`, `SharedAtom<T>`, `select`,
  `select_ref` — fine-grained reactive cells from `kernel::atom`.
  `Atom::set` suppresses redundant notifications via `PartialEq`.
  `select` bridges `Store<S>` to atoms with memoized projection.
- `CrdtSync`, `SyncEvent`, `SyncSubscription` (behind `crdt`
  feature) — bidirectional bridge between `CollectionStore` and
  reactive atoms from `kernel::crdt_sync`. Per-object/edge atoms
  auto-update on CRDT mutations; `import_and_refresh` for peer sync.
- `PrismKernel`, `PrismKernelOptions` — the canonical Layer-1
  orchestration struct from `kernel::prism_kernel`. Composes
  `ObjectRegistry`, `PluginRegistry`, `ConfigRegistry` + `ConfigModel` +
  `FeatureFlags`, `NotificationStore`, `ActivityStore`, `BuilderManager`,
  `ProcessQueue`, and `AiProviderRegistry` with the six built-in plugin
  bundles installed by default. Narrowed port of `studio-kernel.ts` —
  Puck / React / Tauri wiring stays out of `prism-core`.
- `KernelInitializer`, `install_initializers`, `Disposer`,
  `KernelInitializerContext`, `noop_disposer` — the self-installing
  startup-hook runner, generic over the kernel type.
- `HelpEntry`, `HelpRegistry`, `HelpProvider` — context-sensitive help
  system (port of the React `HelpRegistry` per ADR-005). `HelpEntry`
  carries `id`, `title`, `summary`, optional `doc_path` + `doc_anchor`.
  `HelpRegistry` is an `IndexMap`-backed store with `register` /
  `register_many` / `register_provider` / `get` / `get_all` / `search`
  (case-insensitive AND-word match) / `clear`. `HelpProvider` trait
  lets any type contribute help entries (implemented by `HelpEntry`
  itself, `ComponentRegistry`, and available for custom types). Pure
  data, no UI — any crate registers entries and the shell looks them
  up at hover time.

## Module status
Tracked in the per-module `//!` docstrings; canonical list:

| Module | Status | Notes |
|---|---|---|
| `help` | ✅ ported | `HelpEntry` + `HelpRegistry` + `HelpProvider` trait (register/register_provider/search/get/clear). Port of React `HelpRegistry` per ADR-005. 17 unit tests. |
| `design_tokens` | ✅ ported | Leaf, no deps. |
| `shell_mode` | ✅ ported | Pure data + pure functions. |
| `boot_config` | ✅ ported | Uses `shell_mode`. |
| `foundation::batch` | ✅ ported | |
| `foundation::date` | ✅ ported | |
| `foundation::object_model` | ✅ ported | `case_str`, `context_engine`, `edge_model`, `nsid`, `query`, `registry`, `tree_model`, `types`, `weak_ref`. |
| `foundation::geometry` | ✅ new | `Point2`, `Size2`, `Rect`, `Edges<T>` — pure 2D geometry primitives built on `glam`. ADR-003. 13 unit tests. |
| `foundation::spatial` | ✅ new | `Transform2D`, `ComputedTransform`, `Anchor`, `ZIndex`, `TransformModifier`, `snap_to_grid` — the builder's transform system (ADR-003). Uses `glam::Affine2` for local/global coordinate space composition. 12 unit tests. |
| `foundation::undo` | ✅ ported | Borrows manager via `SharedUndoManager` in bridge. |
| `foundation::vfs` | ✅ ported | `VfsAdapter` trait + `MemoryVfsAdapter` + `VfsManager`. `FileSystemVfsAdapter` stores blobs at `{root}/{hash}` with `.meta` sidecar JSON files (mime_type, size, created_at). Content-addressed via SHA-256, idempotent writes, `Mutex` for thread safety. 7 unit tests. |
| `foundation::clipboard` | ✅ ported | `TreeClipboard` borrows tree/edges/undo per-call. |
| `foundation::template` | ✅ ported | `TemplateRegistry` with `{{var}}` interpolation. |
| `foundation::persistence` | ✅ ported | Gated behind the `crdt` feature. `CollectionStore` wraps a `LoroDoc` with `objects` + `edges` maps (records stored as JSON strings so snapshots round-trip with the legacy TS runtime), exposes CRUD + `ObjectFilter` / `EdgeFilter` + synchronous `on_change` listeners + `export_snapshot` / `import`. `VaultManager<A>` orchestrates a `PrismManifest`'s collections against a `PersistenceAdapter` trait; `MemoryAdapter` ships for tests; `FileSystemAdapter` stores Loro snapshots to `{root}/{path}` with auto-created parent dirs (7 unit tests). Lazy-loads stores on first `open_collection`, tracks dirty state via per-store change listeners, saves snapshots to `data/collections/{id}.loro`. 63 unit tests. |
| `identity::did` | ✅ ported | Ed25519 identity, sign/verify, multisig, import/export. |
| `identity::encryption` | ✅ ported | AES-GCM-256 vault key manager, HKDF-derived keys. |
| `identity::manifest` | ✅ ported | Privilege sets + enforcer + `.prism.json` parse/serialise/validate. |
| `identity::trust` | ✅ ported | Sovereign immune system — Luau sandbox, schema poison-pill validator, hashcash proof-of-work, peer trust graph, Shamir secret sharing, encrypted escrow, PBKDF2 password auth. |
| `language::syntax` | ✅ ported | AST types (`SyntaxNode` / `RootNode` / `Position` / `SourceRange` + `pos_at` / `range`), `Scanner` + `ScannerState`, `TokenStream` + `BaseToken`, case utils (`to_camel_case` / `to_pascal_case` / `to_snake_case` / `to_screaming_snake` + `_ident` variants + `safe_identifier`), spell-check consumer types (`SpellChecker` / `PersonalDictionary` / `SpellCheckDiagnostic`), and the LSP-like syntax engine (`DefaultSyntaxEngine`, `create_syntax_engine`, `ExpressionProvider`, `create_expression_provider`, `builtin_functions`, `field_type_map`, `generate_luau_type_def`, `infer_node_type`, plus `Diagnostic` / `CompletionItem` / `HoverInfo` / `LuauTypeDef` / `SchemaContext` / `SignalContext` / `SignalPayloadField` / `SyntaxProvider` types). `SchemaContext` carries `signals: Vec<SignalContext>` for signal-aware provider intelligence. 21 unit tests. |
| `language::expression` | ✅ ported | Tokens (`Token` / `TokenKind` / `OperandData`) + `tokenize` scanner, `parse` Pratt parser producing `AnyExprNode`, evaluator (`evaluate` / `evaluate_expression` / `wrap_bare_identifiers` / `ContextStore` / `EvaluateResult`), field resolver (`build_formula_context`, `read_object_field`, `resolve_computed_field` / `resolve_formula_field` / `resolve_lookup_field` / `resolve_rollup_field`, `aggregate`, `FieldResolverStores` / `ObjectLookup` / `EdgeLookup`), plus `ExprType` / `ExprValue` / `ExprError` / `BinaryOp` / `UnaryOp` / `format_number`. `chrono` powers the ISO-date builtins. 29 unit tests. |
| `language::registry` | ✅ ported | Unified `LanguageContribution` registry (ADR-002 §A2) — `SurfaceMode`, `InlineTokenDef` + builder, wikilink token, `LanguageSurface`, compound-extension resolver. Generic over renderer (`R`) and editor-extension (`E`) types; `()` defaults keep it framework-free. Exposes `LanguageRegistry::resolve_file(&PrismFile)` for direct document→contribution lookup. |
| `language::document` | ✅ ported | ADR-002 §A1 `PrismFile` — unified file record with `FileBody::{Text, Graph, Binary}`, narrowing helpers, and keyword-struct builders (`TextFileParams` / `GraphFileParams` / `BinaryFileParams`). `schema` is now a typed `Option<DocumentSchema>` — the `serde_json::Value` placeholder came out when `language::forms` landed. |
| `language::forms` | ✅ ported | `field_schema`, `document_schema`, `form_schema`, `form_state`, `wiki_link`, `markdown` (Prism's narrow in-house dialect — not CommonMark). Pure data + pure functions; 48 unit tests. Used by `language::document::PrismFile::schema` and the `language::markdown` contribution below. |
| `language::luau` | ✅ ported | ADR-002 §A4 / Phase 4. `create_luau_contribution()` returns a `LanguageContribution<R,E>` with `parse` (full-moon 2.2.0 parser producing real AST), `syntax_provider` (`LuauSyntaxProvider` with diagnostics, completions, hover — including signal-aware `on_{signal}` handler completions and hover when `SchemaContext.signals` is populated), and `code` + `preview` + `visual_script` surfaces. Submodules: `parser.rs` (14 tests), `provider.rs` (13 tests), `visual.rs` (`LuauVisualLanguage` — bidirectional graph↔source with signal handler decompile/compile via `EventListener` node kind, 14 tests). `mlua`-backed execution lives in `prism-daemon::modules::luau_module`. |
| `language::symbol_index` | ✅ new | IDE-mode Phase 2. `Symbol` / `SymbolKind` (Function / Local / Field) + `index_luau_source(path, src)` walks `full_moon` top-level stmts (functions, `local`s, `M.f = fn` fields) recording each name's byte offset + line/col for jump-to-definition. `SymbolIndex` is a path-keyed store: `rebuild_file` / `remove_file` / `clear` / `lookup` (exact + bare-segment) / `fuzzy` (subsequence scorer for the Ctrl+T palette). Parse failure → no symbols (mirrors `luau::top_level_locals`). 8 unit tests. |
| `language::visual` | ✅ ported | Generalized visual↔textual bridge for bidirectional code/graph editing. `ScriptGraph` IR (nodes, edges, topo sort, validation), `ScriptNode` (25 node kinds), `PortDef` (execution/data ports), `VisualLanguage` trait (`decompile` source→graph, `compile` graph→source, `node_palette`, `validate`). Language-agnostic — Luau is the first impl, designed for reuse by Loom Lang and future languages. 7 graph tests + 1 bridge trait test. |
| `language::markdown` | ✅ ported | `create_markdown_contribution()` returns a `LanguageContribution<R,E>` whose `parse` runs `language::forms::markdown::parse_markdown` and projects each block into a child `SyntaxNode` (`hr` / `h1` / `p` / `oli` / `task` / `code` / …). Surface defaults to `preview`, exposes `code` + `preview`, ships the registry wikilink inline token. |
| `language::prism_ui` | ✅ ported + Phase 2 canonical | The `.prui` DSL `LanguageContribution`. `ast.rs` defines `Document` / `Element` / `Attribute` / `AttributeName` / `AttributeNamespace` / `AttributeValue` (+ `TemplatePart`) / `Expression` / `Node` / `ParseError` plus `STATE_SUFFIXES` and `split_state_suffix`. `grammar/mod.rs` exposes `parse(source)` / `parse_xml(source)` / `parse_to_root(source)` — `parse` is the **Phase 2 dispatcher** (`docs/dev/prui-expressiveness-roadmap.md` §6 + §8): first non-whitespace token picks the reader — `<` opens the historic XML parser, a canonical declaration keyword (`component` / `trait` / `mixin` / `macro` / `class` / `type` / `fn` / `let` / `import` / `namespace`) opens the new recursive-descent canonical reader (`grammar/canonical.rs`). Both produce the same `Document` shape; downstream consumers see one AST. `grammar/migrate.rs` ships `rewrite_xml_to_canonical(source) -> (String, Vec<ParseError>)` — the §6.24 cheatsheet's mechanical XML → canonical rewriter that powers `prism rewrite-canonical`. The legacy XML reader is preserved verbatim through the Phase 17 deprecation window. `contribution.rs` exposes `create_prism_ui_contribution()` + `PRISM_UI_ID` + `PRISM_UI_EXTENSIONS`; `provider.rs` (`PrismUiSyntaxProvider`) handles diagnostics / tag completions / attribute-namespace hovers. 65+ unit tests covering both surfaces + the round-trip cheatsheet. |
| `language::codegen` | ✅ ported | ADR-002 §A3 unified pipeline. `CodegenPipeline` dispatches heterogeneous emitters by an open `input_kind` string; `CodegenInputs` is a typed slot bundle backed by `Box<dyn Any>`. Symbol DSL: `SymbolDef` / `SymbolKind` / `SymbolParam` / `EnumValue` + `constant_namespace` / `fn_symbol` builders. Four concrete emitters — `SymbolTypeScriptEmitter`, `SymbolCSharpEmitter`, `SymbolEmmyDocEmitter`, `SymbolGDScriptEmitter` — plus `ts_name_transform` / `cs_name_transform` / `default_gdscript_name_transform`. `SourceBuilder` (line/indent/block/const_block) is the shared text buffer. `TextEmitter` trait rounds `RootNode` back to source text. 30 unit tests. |
| `kernel::store` | ✅ ported | `Store<S>` + `Action<S>` trait + `Subscription` handle. Replaces `zustand` per §6.1 of the migration plan and satisfies §7's hot-reload constraints (one root struct, no global mut, serde-backed `snapshot` / `restore`). Backs `prism_shell::Shell`. 16 unit tests. |
| `reactive::net` | ✅ Phase 7 | Network-scoped reactive primitives, all built on the `RemoteSignal<T>` / `RemoteState<T>` substrate. `FederatedSignal<T>` (cross-relay federation bus — `subscribe` / `publish` / `ingest_remote` / `mark_disconnected`), `PeerSignal<T>` (WebRTC data channel — `subscribe` / `send` / `ingest_remote` / `mark_peer_offline`), `RelaySignal<T>` (one-writer-many-reader relay-managed push stream — `subscribe` / `write` / `ingest_push`). Each scope's actual wire integration is owned by the host (the federation / signaling / relay modules already live in `network::*`); this layer abstracts them behind injectable `FederationTransport` / `PeerTransport` / `RelayTransport` traits so call sites stay test-friendly. `MockFederationTransport` / `MockPeerTransport` / `MockRelayTransport` ship for unit tests. **Production-shape fan-out hub:** `LocalHub` is a channel-keyed fan-out registry (subscribe/publish/unsubscribe over `IpcPayload`); `HubFederationTransport` (`FederationTransport` impl over a `LocalHub`) actually delivers messages to subscribers — composes with real wire transports by having their receive loop call `hub.publish(topic, payload)`. The hub is the reference impl every WebSocket/WebRTC transport mirrors. Drop semantics: signals call their transport's `close` on drop so subscriptions tear down deterministically. 17 unit tests. |
| `reactive::ipc` | ✅ Phase 6 | Cross-process reactive primitives. `RemoteState<T>` enum (`Loading` / `Live` / `Stale { value, since_ms }` / `Errored { last, err }`) — the connectivity-aware carrier from §8 of `docs/dev/dioxus-inspiration.md`. `RemoteError` + `RemoteSignal<T>` wrapping `Signal<RemoteState<T>>` with `read` / `last_known` / `try_read` / `state` / `is_live` / `set_state` / `set_live` / `set_errored`. `DaemonInvoker` trait — abstract transport seam for `#[daemon_fn]` client stubs; `MockInvoker` test seam. `DaemonFnError<E>` typed error carrier returned by every emitted `<name>_client` stub. 13 unit tests in core + 5 integration tests in `prism-daemon/tests/daemon_fn_macro.rs` exercising the macro end-to-end against `MockInvoker` and the real `CommandRegistry`. |
| `luau_reactive` (gated `luau`) | ✅ Phase 5 | Hand-rolled `mlua::UserData` impls for `Signal<Value>` (`read` / `peek` / `track` / `write` / `set`) and `Memo<Value>` (`read` / `peek`). Carries `serde_json::Value` because Luau is dynamically typed and the mlua serde bridge already speaks JSON; typed `Signal<T>` stays Rust-only. Type stubs live in `luau_bindings_consts` (`REACTIVE_SIGNAL_TYPE_NAME` / `REACTIVE_SIGNAL_TYPE_DEF` + the `_MEMO_` pair), always-compiled so codegen picks them up without the `luau` runtime feature. `luau_types::type_defs` registers both. Lua-side **construction** of signals/memos/effects (`prism.reactive.signal(0)`, `effect(function() ... end)`) is a sibling host concern (lives next to the Lua state lifecycle in `prism-daemon::modules::luau_module`) — this module only handles the Rust→Luau handoff. 8 unit tests under `mlua::Lua`. |
| `reactive` | ✅ Phase 1 + 2 bridge + Phase 3 scaffold | `Signal<T>` (`Copy + 'static` handle over `generational-box`) with subscribing `read`/`get`/`track` + non-subscribing `peek`/`snapshot` + `set`/`write`/`try_set`. `Memo<T>` with PartialEq gating + chained derivations. `Effect` with `Drop`-based disposal + auto re-tracking via `ReactiveContext::reset_and_run_in`. `Owner` + `ReactiveContext` thread-local stack + on-stack-guard cycle breaker in `mark_dirty`. `Owner::insert_effect` / `Owner::insert_memo` retain reactive scopes for the owner's lifetime so dropping the owner disposes everything inside (the Phase 1 "Effects drop when their owner drops" guarantee). Source `Location` carried via `#[track_caller]` on `ReactiveContext::new` / `Effect::new` / `Memo::new` / `Owner::insert_effect` / `Owner::insert_memo`, queryable via `ReactiveContext::source` / `Effect::source` / `Memo::source` — Phase 3 (render-scope NodeId mapping) and Phase 9 (subsecond hot-reload diagnostics) feed off this. `DirtyQueue<K>` dedup'd flush primitive for render-scope / SSR-scope / IPC-scope batching (Phase 3 scaffold). `ReactiveContext::batch(\|\| { ... })` (Phase 8 open-Q resolution, 2026-05-13) defers `mark_dirty` notifications until the outermost scope exits — `CrdtSync::{process_changes, refresh_all}`, `services::signals::fire_signal`, `events::dispatch_event`, and every `#[daemon_fn]` handler wrap their fan-out in it so multi-write transactions wake each dependent reactive scope exactly once. `Resource<T>` + `ResourceState<T>` (`Pending` / `Loading { last }` / `Ready(T)` / `Errored { last, err }`) — executor-agnostic async carrier with `start_load` / `resolve` / `fail` transitions; loading preserves the last `Ready` snapshot so spinner-over-stale-data UIs work via one `last_known()` accessor. **Naming**: the user-facing reactive cell is always spelled `reactive::Signal<T>` to disambiguate from `prism_builder::signal::SignalDef` (user-authored event channels) — see §5 of `docs/dev/dioxus-inspiration.md`. Of the Dioxus-inspired reactive overhaul — Phases 0/1/2/3-scaffold landed plus the Phase 8 batch + Resource follow-ups; Phases 3 (full render-scope wiring), 4 (reactive props), 5 (Luau), 6 (`#[daemon_fn]` body), 7 (network signals), 8 (SSR signals) remain. 36 unit tests. |
| `kernel::atom` | ✅ Phase 2 (rebuilt on reactive::Signal) | `Atom<T>` is now a `Clone`-cheap (`Rc<AtomInner<T>>` internal) wrapper over an `Owner` + `Signal<u64>` notification tick + `RefCell<T>` value + direct-listener `Vec`. Two independent subscriber channels: (1) `subscribe(callback)` — the legacy listener API (`FnMut(&T)` fired on every mutation) for host shells / IPC clients; (2) `read` / `track` / `signal()` — reactive auto-subscription via the inner tick signal so any `Effect` / `Memo` reading the atom wakes on mutation. All mutating methods take `&self`. `set` checks `PartialEq` and suppresses redundant notifications across both channels; `update` / `force_notify` for in-place mutation. **`SharedAtom<T>` typedef retired** — `Atom<T>` itself is the share-by-clone handle. `Atom::reactive_signal(&owner)` mirrors the value into a fresh `Signal<T>` allocated in another owner (cross-owner bridge). `select(store, selector) -> Atom<T>` bridges `Store<S>` projection to an atom — installs a store subscriber that projects a field and only fires atom subscribers when the projection changes. `select_ref` variant for reference-returning selectors. **`select_memo(store, owner, selector) -> Memo<T>`** is the new reactive-substrate path: same projection semantics but the result speaks `Memo<T>` (read inside an `Effect` to subscribe). 26 unit tests. |
| `kernel::crdt_sync` | ✅ Phase 2 | Bidirectional bridge between `CollectionStore` (Loro CRDT) and the reactive `Atom` layer. `CrdtSync` wraps a shared `CollectionStore` and maintains per-object / per-edge `Atom<T>`s (now `Clone`-cheap, no `Rc<RefCell<…>>` wrapper) that auto-update on mutations. All writes flow through `write_object` / `write_edge` (CRDT-first, then atom sync). `object_signal` / `edge_signal` mirror the tracked atom into a caller-supplied `reactive::Owner`, returning a `Signal<T>` that wakes any reactive subscriber on CRDT mutation — the "Loro → reactive graph" half of Phase 2. `import_and_refresh` for remote peer snapshots. `process_changes` for external mutations. `SyncEvent` bus for global change notification. Gated behind the `crdt` feature. 19 unit tests. |
| `kernel::state_machine::machine` | ✅ ported | Flat, context-free finite state machine from `kernel/state-machine/machine.ts`. Generic over `State + Event` (`Eq + Hash + Clone`), `TransitionFrom::{One, Many, Any}` source matching, guards + actions, enter/exit hooks, terminal states, wildcard transitions, opaque `Subscription` handles. The xstate-backed `tool.machine.ts` is deferred to a `statig` rewrite and is not exported. 23 unit tests. |
| `kernel::config` | ✅ ported | Layered config system (port of `kernel/config/*`). `SettingScope::{Default, Workspace, User}` cascade (most specific wins; `app` / `team` dropped — Prism is local-first). `ConfigRegistry` with the 17 built-in settings (UI / editor / sync / AI / notifications) and the `ai-features` + `sync` feature flags. `ConfigModel` with `get` / `set` / `reset` / `load` / `watch` / `on_change` / scope resolution / secret masking / pluggable `ConfigStore` trait (`MemoryConfigStore` ships in-crate; file-backed stores live in host crates). Pluggable JSON Schema subset validator (`StringSchema` / `NumberSchema` / `BooleanSchema` / `ArraySchema` / `ObjectSchema`, `validate_config`, `coerce_config_value`, `schema_to_message`) — `pattern` dropped on purpose, no built-in setting needed it and avoiding user-regex evaluation keeps the module leaf. `FeatureFlags` bound to the model's `on_change` bus, re-firing flag watchers when their `setting_key` mutates. `ConfigModel` is `Clone`-as-handle (inner state + watchers + listeners share `Rc<RefCell<_>>`), and dispatches events *after* releasing the inner borrow so listener callbacks can read/mutate config during dispatch. 34 unit tests. |
| `interaction::notification` | ✅ ported | Runtime notification registry + debounced dedup queue (port of `interaction/notification/*`). `NotificationStore` uses an `IndexMap` for insertion-ordered state; supports `add` / `mark_read` / `mark_all_read` / `dismiss` / `dismiss_all` / `pin` / `unpin` / `get` / `get_all` (with `NotificationFilter`) / `get_unread_count` / `hydrate` / `clear_dismissed` / `subscribe`. Eviction policy: dismissed-unpinned (oldest first) → read-unpinned (oldest first); pinned and unread are preserved. `NotificationQueue` debounces + dedups by `object_id:kind` key and is timer-agnostic via a `TimerProvider` trait (`SystemTimer` ships; `FakeTimer` used in tests). 26 unit tests. |
| `interaction::activity` | ✅ ported | Append-only activity log + per-object formatter / date-bucketing (port of `interaction/activity/*`). `ActivityVerb` enum (20 variants + `Custom`), `FieldChange { before/after: JsonValue }`, `ActivityEvent` with `from_parent_id: Option<Option<String>>` distinguishing "not applicable" vs "moved from root vs id". `ActivityStore` keeps a per-object ring buffer (default cap 500) + per-object `ActivitySubscription` listeners. `formatter::{format_field_name, format_field_value, format_activity, group_activity_by_date_at}` returns `ActivityDescription { text, html }` with `<b>`-wrapped highlights and Today / Yesterday / This week / Earlier buckets. The legacy `activity-tracker` was deliberately **not** ported — the Rust `CollectionStore` doesn't expose per-object subscriptions yet, so tracking belongs to the host. 31 unit tests. |
| `interaction::query` | ✅ ported | Pure filter / sort / group pipeline over `GraphObject` (port of the useful half of `interaction/view/view-config.ts`). `FilterOp::{Eq, Neq, Contains, Starts, Gt, Gte, Lt, Lte, In, Nin, Empty, Notempty}`, `FilterConfig`, `SortConfig { field, dir: SortDir }`, `GroupConfig`, `GroupedResult`. `Query { filters, sorts, groups, columns, limit, exclude_deleted }` + `apply_filters` / `apply_sorts` / `apply_groups` / `apply_query` + `get_field_value` (resolves TS-compatible shell fields like `parentId` / `endDate` / `createdAt`, falls back to `data` payload). Deliberately renamed from `view` — every view is a `prism_builder::Component`, never a `ViewMode` enum; `SavedView` / `LiveView` are intentionally **not** ported. 17 unit tests. |
| `domain::flux` | ✅ ported | Layer-1 Flux registry (port of `domain/flux/*`). Entity/edge/automation defs for the Flux life-OS: 11 entity types (`task`, `project`, `goal`, `habit`, `note`, `note_page`, `person`, `meeting`, `health`, `finance`, `location`) with full `EntityDef` + field schemas and `UiHints`; 7 edge types (`parent_child`, `blocks`, `assignee`, `attendee`, `related_note`, `links_to`, `part_of`) with `EdgeBehavior`; 8 automation presets (`FluxAutomationPreset` + `FluxAutomationAction { kind, target, value }` + `FluxTriggerKind`). `FluxRegistry` exposes `get_entity_defs` / `get_entity_def` / `get_edge_defs` / `get_edge_def` / `get_automation_presets` / `get_presets_for_entity` / `export_data` (CSV + JSON via `FluxExportFormat` / `FluxExportOptions`) / `parse_import` (returns `FluxImportResult`, errors via `FluxImportError`). `ObjectMap = IndexMap<String, Value>` preserves insertion order through import/export. Status tables (`TASK_STATUSES`, `PROJECT_STATUSES`, etc.) are compile-time `&'static [StatusOption]`. 38 unit tests. |
| `domain::timeline` | ✅ ported | Layer-1 NLE / show-control timeline engine (port of `domain/timeline/*`). `TimelineEngine` owns `Vec<TimelineTrack>` (audio / video / lighting / automation / midi), markers, transport state, and a `Box<dyn TimelineClock>`. Transport: `play` / `pause` / `stop` / `seek` / `scrub` / `set_speed` / `set_loop` / `get_transport`. Track CRUD: `add_track` / `remove_track` / `update_track` (partial via `TrackUpdates`) / `get_track(s)`. Clip CRUD: `add_clip` / `remove_clip` / `move_clip` / `trim_clip` / `get_clip` — locked-track and locked-clip guards match the TS engine. Automation: `add_automation_lane` / `remove_automation_lane` / `add_automation_point` / `remove_automation_point` / `get_automation_value` with step / linear / Bézier (smoothstep) interpolation. Markers: `add_marker` / `remove_marker` / `get_markers`. Queries: `get_duration` / `get_clips_at_time`. Events via `subscribe(Box<dyn FnMut(&TimelineEvent)>) → u64` handle + `unsubscribe`. `TempoMap` (PPQN, tempo markers, time signatures) supports `tempo_at` / `to_musical` / `to_seconds` / `add_tempo` / `set_time_signature`. `ManualClock` (the testing clock) impls the `TimelineClock` trait and supports `advance` / `set_time` + fires scheduled callbacks in time order. `thread_local! Cell<u64>` counter for IDs (`reset_id_counter()` in tests). No audio/video I/O — that lives in `prism-daemon`. 67 unit tests. |
| `domain::graph_analysis` | ✅ ported | Layer-1 pure graph analysis over `GraphObject` arrays (port of `domain/graph-analysis/*`). `dependency_graph`: `build_dependency_graph` / `build_predecessor_graph`, Kahn's `topological_sort` (appends cyclic nodes at end), `detect_cycles` via iterative DFS with in-stack tracking, `find_blocking_chain` + `find_impacted_objects` (BFS upstream / downstream), `compute_slip_impact` (BFS wave propagation with max-slip conservation, sorted by depth then name) → `SlipImpact { object_id, object_name, slip_days, depth }`. `planning_engine`: `compute_plan` runs a full Critical Path Method forward / backward pass over `topological_sort` output, returning `PlanResult { total_duration_days, critical_path, nodes: BTreeMap<String, PlanNode> }`. Duration priority: `data.durationDays` → `data.estimateMs` → `date` / `end_date` span (parsed via `chrono`) → default 1. Reads predecessors from both `data.dependsOn` and `data.blockedBy`. 30 unit tests. |
| `kernel::actor` | ✅ ported | ADR-002 §Part C process queue (port of `kernel/actor/*` minus the AI bits, which moved to `kernel::intelligence`). `ProcessQueue` owns a priority-ordered `VecDeque<QueuedProcess>` + `HashMap<String, Arc<dyn ActorRuntime>>` runtime map + per-queue subscription bus. `ActorRuntime` trait: `name() -> &str`, `run(&self, ctx: ActorContext) -> ActorResult`, `capabilities() -> CapabilityScope`. Synchronous `process_next` / `process_all` / `enqueue` / `cancel` / `pending_count` / `subscribe` (deviation from TS async: Rust hosts pump the queue from their own runtime). `TestRuntime` ships for unit tests. `CapabilityScope` enforces per-runtime allow-lists before dispatch. 14 unit tests. |
| `kernel::intelligence` | ✅ ported | ADR-002 §Part C AI provider split (port of `kernel/actor/ai*` + `kernel/context/*`). `AiProviderRegistry` tracks registered `Arc<dyn AiProvider>` instances with `register` / `unregister` / `set_active` / `active` / `get` / `list`. `AiProvider` trait: `id()`, `label()`, `complete(&self, request: CompletionRequest) -> CompletionResponse`, `stream(...)`. Three providers: `OllamaProvider` (local HTTP), `ExternalProvider` (OpenAI-compatible), `TestAiProvider` (deterministic fixtures for tests). `AiHttpClient` trait abstracts over the HTTP backend so `prism-daemon` can inject `reqwest` while tests use an in-memory fake. `ContextBuilder` assembles `AiContext` from a `GraphObject` + surrounding neighbourhood for prompt templating. 11 unit tests. |
| `kernel::automation` | ✅ ported | Trigger / condition / action engine (port of `kernel/automation/*`). `AutomationEngine::new(store: Arc<dyn AutomationStore>, handlers: HashMap<String, Arc<dyn ActionHandler>>, options)` — host supplies both, which is why `PrismKernel` does not wire one in by default. `AutomationStore` / `ActionHandler` traits, `DelaySleeper` trait for scheduled delays (`SystemSleeper` + `FakeSleeper` ship). Condition evaluator walks `{{object.field.nested}}` paths, supports string/number/bool comparisons, and interpolates `{{…}}` templates into action payloads. Cron-style `tick(now_ms)` entry point lets hosts drive the engine from their own scheduler. 24 unit tests. |
| `kernel::plugin` | ✅ ported | ADR-002 §Part C contribution fan-out (port of `kernel/plugin/*`). `PluginRegistry` wraps four inner `ContributionRegistry<T>` buckets — views, commands, keybindings, context menus — each with `register` / `unregister` / `get` / `list` + a synchronous listener bus. `PrismPlugin` trait carries `id` / `name` / `version` + an `install(&self, ctx: &mut PluginInstallContext)` hook. Registration is idempotent by id; re-registering replaces. `has(id)` / `len()` / `names()` round out the query surface. 7 unit tests. |
| `kernel::plugin_bundles` | ✅ ported | Six built-in `PluginBundle`s + a `flux_types` submodule (port of `kernel/plugin-bundles/*`). `create_builtin_bundles() -> Vec<Box<dyn PluginBundle>>` returns `work` / `finance` / `crm` / `life` / `assets` / `platform`, each seeding entity defs into the `ObjectRegistry` and plugin contributions into the `PluginRegistry`. `install_plugin_bundles(&bundles, &mut ctx)` is the fan-out helper; `PluginInstallContext<'a> { object_registry, plugin_registry }` is the mutable context it threads through. `flux_types` factors shared Flux entity schemas so the `work` / `life` / `finance` bundles can reuse them without pulling in `domain::flux`. 8 unit tests. |
| `kernel::builder` | ✅ ported | Self-replicating profile / build-plan manager (port of `kernel/builder/*`). `BuilderManager` owns `Vec<AppProfile>` + `Arc<dyn BuildExecutor>` + `Vec<BuildRun>` history. Profile CRUD: `add_profile` / `remove_profile` / `update_profile` / `get_profile` / `list_profiles`. Planning: `plan_build(profile_id, target: BuildTarget, dry_run: bool) -> BuildPlan` emits a deterministic `Vec<BuildStep>` (`EmitFile` / `RunCommand` / `InvokeIpc`). Execution: `run_build(plan) -> BuildRun` dispatches through the `BuildExecutor` trait — two concrete impls ship: `DryRunExecutor` (returns success without touching the filesystem) and `CallbackExecutor` (wraps an IPC closure for hosts talking to `prism-daemon`). Six builtin profiles (studio / flux / lattice / cadence / grip / relay) via `BuiltInProfileId` enum + `create_builtin_profiles()`. `materialize_starter_app(&profile, create_object_fn: StarterCreateObjectFn)` seeds a fresh workspace's app-shell + page-shell + route tree from a `StarterAppTemplate`. 22 unit tests. |
| `kernel::initializer` | ✅ ported | Self-installing startup-hook runner (port of `kernel/initializer.ts`). `KernelInitializer<TKernel>` generic trait: `id()` / `name()` / `install(&self, ctx: KernelInitializerContext<'_, TKernel>) -> Disposer`. `Disposer = Box<dyn FnOnce()>`. `install_initializers(initializers, kernel) -> Disposer` installs a slice in order and returns a composite disposer that tears them down in reverse. `noop_disposer()` for initializers with no cleanup. Generic over the kernel type so `PrismKernel` and host-specific kernels share the same contract. 3 unit tests. |
| `kernel::prism_kernel` | ✅ ported | Canonical Layer-1 orchestration struct (narrowed port of `prism-studio/src/kernel/studio-kernel.ts`). `PrismKernel { object_registry, plugin_registry, config_registry: Rc<ConfigRegistry>, config_model, feature_flags, notification_store, activity_store, builder_manager, process_queue, ai_provider_registry }`. `PrismKernelOptions` knobs: `config_registry`, `install_builtin_bundles` (default `true`), `builder_executor` (default `create_dry_run_executor()`), `builder_profiles`. `install_initializers(&[Arc<dyn KernelInitializer<PrismKernel>>]) -> Disposer` delegates to the generic runner. Inherently single-threaded — `ConfigModel` / `FeatureFlags` / `ActivityStore` are `!Send` by design; multi-threaded hosts wrap the kernel in their own sync layer. Intentional omissions: `CollectionStore` (gated on the `crdt` feature, host-owned), `AutomationEngine` (requires host-supplied `AutomationStore` + `ActionHandler`s), domain engines (`FluxRegistry` / `TimelineEngine` / graph analysis are stateless or per-document), and the `network::*` subtree (composes differently per host). 10 unit tests. |
| `network::presence` | ✅ ported | Real-time collaborative presence tracker (port of `network/presence/*`). `PresenceManager` owns local peer snapshot + `IndexMap` of remote peers, TTL-expires stale entries via pluggable `TimerProvider`. `PeerIdentity` / `CursorPosition` / `SelectionRange` / `PresenceState` data types. `PresenceChange` events (`Joined` / `Updated` / `Left`) via subscription bus. `set_cursor` / `set_selections` / `set_active_view` / `set_data` / `update_local` for local state, `receive_remote` / `remove_peer` for remote peers, `sweep` for TTL eviction. `SystemTimer` ships for real callers; tests inject `MockTimer`. 39 unit tests. |
| `network::relay` | ✅ ported | Relay connection types, transport abstractions, **composable module system**, and **17 relay modules**. Core types: `RelayId` / `RelayProtocol` / `RelayEndpoint` / `RelayInfo` / `RelayConfig`, `RelayEnvelope` / `RelayMessageKind` / `RelayRequest` / `RelayResponse` / `RelayError`, `Transport` trait + `RelayConnection` state machine. Module system (`module_system.rs`): `RelayContext` (capability registry via `Arc<dyn Any + Send + Sync>`), `RelayModule` trait, `RelayBuilder` (dependency validation at build time), `RelayInstance`. 17 modules in `modules/`: `blind_mailbox` (E2EE store-and-forward), `relay_router` (online/offline routing), `timestamper` (SHA-256 receipts), `blind_ping` (push notification transport), `capability_tokens` (issue/verify/revoke), `webhooks` (HMAC-SHA256 signed delivery), `sovereign_portals` (registry), `signaling` (WebRTC rooms), `collection_host` (CRDT snapshots), `vault_host` (owner-authed blob store), `hashcash` (proof-of-work gate), `peer_trust` (thread-safe reputation + content flagging), `escrow` (deposit/claim lifecycle), `federation` (cross-relay forwarding), `password_auth` (PBKDF2-SHA256), `acme` (certificate management), `portal_templates` (CRUD). All thread-safe (`Send + Sync`). 29+ unit tests. |
| `network::relay_manager` | ✅ ported | Connection manager for multiple relays (port of `kernel/relay-manager.ts`). `RelayManager` orchestrates a pool of `ManagedRelay`s with health tracking (`RelayHealth`: latency, failures, message counts), status derivation (`RelayStatus`: idle / connecting / active / degraded / unavailable / disabled), and per-role primary selection. `RelayPolicy` / `RelaySelector` for relay selection strategies: `FirstAvailable` / `RoundRobin` / `LowestLatency` / `Random` / `Fixed`, with tag filtering and failure thresholds. `RelayManagerEvent`s (`RelayStatusChanged` / `RelayConnected` / `RelayDisconnected` / `PrimaryChanged` / `AllUnavailable` / `Recovered`). Host-driven lifecycle via `process_relay_connected` / `process_relay_disconnected` / `process_relay_state_changed` / `process_ping_response`. 23 unit tests. |
| `network::discovery` | ✅ ported | `VaultRoster` (peer/relay address book with TTL sweep, trust status, health tracking, export/import) + `DiscoveryService` (multi-vault coordinator, relay directory ingestion, peer announcement processing, event bus). 27 unit tests. |
| `network::session` | ✅ ported | `SessionManager` (create/end/pause/resume, join/leave tracking, operation/message recording, transcript access), `TranscriptTimeline` (append-only ordered log with max-entries eviction, seq/time/peer/kind queries, export/import), `PlaybackController` (variable-speed replay with play/pause/stop/seek/tick, progress tracking). 39 unit tests. |
| `network::server` | ✅ ported | `generate_route_specs(registry)` walks `ObjectRegistry` and emits `Vec<RouteSpec>` — 5 CRUD routes per entity type + 3 relationship routes per edge type. `generate_openapi(registry, title, version)` produces a full OpenAPI 3.0.3 spec with JSON Schema components derived from `EntityFieldDef` field lists (type mapping, enum values, required fields, format annotations). Pure data — host crates wire specs to their HTTP framework. 22 unit tests. |

When porting a new module, match the leaf-first order: port the
dependency-free pieces first, snapshot-test with `insta`, then layer
the higher-level modules on top. Keep per-module `//!` docs honest
about status — the table above is generated by hand and drifts.

## Style
- No multi-paragraph docstrings on items; `//!` module headers are
  allowed because they carry status and dependency notes.
- Types that need to survive hot-reload must live inside the module
  they're defined in — no global `lazy_static` / `OnceCell`.

## Downstream
`prism-builder`, `prism-shell`, and `prism-studio/src-tauri` all depend
on `prism-core`. Touch its public surface carefully and re-run
`cargo check --workspace` as the safety net.
