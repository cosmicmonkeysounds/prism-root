//! Native femtovg backend — Phase 1 stub.
//!
//! Lowers `Vec<RenderCommand>` onto a winit window via femtovg.
//! Implementation lands with the Phase 1 runtime spike (see
//! `docs/dev/clay-migration-plan.md`); this stub keeps `cargo fmt`
//! and the cargo feature gate honest until then.
